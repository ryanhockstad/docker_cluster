{
  "index_patterns": ["logstash_panos-*"],
  "template": {
    "settings": {
      "number_of_shards": 1,
      "number_of_replicas": 0,
      "index.lifecycle.name": "example",
      "index.lifecycle.rollover_alias": "logstash_panos"
    }
  }
}